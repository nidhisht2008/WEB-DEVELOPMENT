<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <!-- If no recipe yet, show: -->
  <% if (!locals.recipe){ %>
    <h2>Pick your favourite taco ingredient 👆</h2>
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
  <% }else{%>
    <h3>Ingredients:</h3>
    <%JSON.parse(recipeJSON).forEach((ingredient)=>{ %>
      <% if(ingredient.name==recipe.charAt(0).toUpperCase()+recipe.slice(1)+" Taco"){%>
        <ul id="ingredientsList">
          <li><%= ingredient.ingredients.protein.name %>, <%= ingredient.ingredients.protein.preparation %></li>
          <li><%= ingredient.ingredients.salsa.name %></li>
          <% ingredient.ingredients.toppings.forEach((topping)=>{ %>
            <li><%= topping.quantity%> of <%= topping.name %></li>
          <% }) %>
        </ul>
      <% } %>
    <% }) %>
  <% } %> 
  </div>
</body>

</html>